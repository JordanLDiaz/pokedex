export class MyPokemon {
  constructor(data) {
    this.name = data.name,
      this.weight = data.weight,
      this.nickName = data.nickName,
      this.img = data.img
    this.height = data.height
  }

  get MyPokemansTemplate() {
    return `
    // TODO finish this...find step 31 in notes.
    `
  }
}